rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Temporary setup rules - ALLOWS ALL WRITES
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
